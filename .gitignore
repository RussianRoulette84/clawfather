# Environment variables (secrets, API keys — never commit)
.env
.env.sensitive
.env.*
!.env.example
!.env.sensitive.example

# Private dir
private/

# User config (wizard writes here; paths, model choices)


# Dependencies
node_modules/
dist/
build/

# IDEs
.idea/
# Ignore .vscode/settings.* except settings.json (only project settings committed)
.vscode/settings.*
!.vscode/settings.json
*.swp
.DS_Store

# OpenClaw runtime (sessions, identity, credentials, tokens — never commit)
.openclaw/
*.log
log
logs/

# Python (skills)
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

# Temp
.tmp_sync/

# Skills: ignore downloaded skill subdirs; keep root /*.* (e.g. CLAWHUB_SKILLS.md, SYNC_SKILLS_README.md)
skills/*/

# Reference (Dockerfiles / examples — not used by Clawfather run)
reference/
